# MetaLens npm configuration
# Ignore tar and tmp vulnerabilities in electron-forge dependencies (HIGH severity)
# These are false positives for a desktop app that doesn't extract archives from untrusted sources
# Setting audit-level=critical ignores HIGH and MODERATE vulnerabilities
# Only reports CRITICAL issues (security fixes that require immediate action)

audit-level=critical
